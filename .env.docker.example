# Miro MCP - Docker/Gateway deployment
# All credentials must be base64-encoded

# Generate with: echo -n "your_value" | base64
MIRO_CLIENT_ID_B64=
MIRO_CLIENT_SECRET_B64=
MIRO_ACCESS_TOKEN_B64=
MIRO_REFRESH_TOKEN_B64=

# To encode existing tokens from token.json:
# export MIRO_ACCESS_TOKEN_B64=$(jq -r '.access_token' token.json | base64)
# export MIRO_REFRESH_TOKEN_B64=$(jq -r '.refresh_token' token.json | base64)
