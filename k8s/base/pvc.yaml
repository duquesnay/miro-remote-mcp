apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: miro-mcp-tokens
  labels:
    app.kubernetes.io/name: miro-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: scw-bssd
