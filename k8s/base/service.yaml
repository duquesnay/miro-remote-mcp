apiVersion: v1
kind: Service
metadata:
  name: miro-mcp
  labels:
    app.kubernetes.io/name: miro-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: miro-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
